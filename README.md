# Ресурстарды брондау жүйесі (Booking & Reservation System)

Ресурстарды басқару және брондау үшін арналған таратылған микросервистік жүйе. Spring Boot, Keycloak, PostgreSQL, RabbitMQ, Redis және Docker технологиялары қолданылады.

## 📋 Мазмұны

- [Архитектура](#архитектура)
- [Функционалдық мүмкіндіктер](#функционалдық-мүмкіндіктер)
- [Технологиялар](#технологиялар)
- [Қажетті құралдар](#қажетті-құралдар)
- [Орнату және іске қосу](#орнату-және-іске-қосу)
- [Конфигурация](#конфигурация)
- [API Эндпоинттер](#api-эндпоинттер)
- [Мысалдар](#мысалдар)
- [Сервистерге қолжетімділік](#сервистерге-қолжетімділік)
- [Мәселелерді шешу](#мәселелерді-шешу)

## 🏗️ Архитектура

Жүйе микросервистік архитектураға негізделген, әрбір сервис өз міндетін орындайды.

### Микросервистер:

#### 1. API Gateway (Порт 8080)
- **Рөлі**: Біртұтас кіру нүктесі
- **Функциялары**:
  - Барлық клиенттік сұрауларды қабылдау
  - Микросервистерге маршрутизация
  - Аутентификация және авторизацияны тексеру
  - CORS баптауларын басқару
  - Запростарды логирлеу

#### 2. Resource Service (Порт 8081)
- **Рөлі**: Ресурстарды басқару (бөлмелер, жабдықтар, көліктер және т.б.)
- **Функциялары**:
  - Ресурстарды құру, оқу, жаңарту, жою (CRUD)
  - Ресурстарды сүзгілеу (тип, қолжетімділік бойынша)
  - Ресурстардың қолжетімділігін тексеру
  - Redis кешімен жұмыс
  - MinIO арқылы файлдармен жұмыс

#### 3. Booking Service (Порт 8082)
- **Рөлі**: Брондауларды басқару
- **Функциялары**:
  - Брондауларды құру
  - Пайдаланушының брондауларын көрсету
  - Брондауларды болдыру
  - Автоматикалық бағаны есептеу
  - Resource Service-пен интеграция

#### 4. Notification Service (Порт 8083)
- **Рөлі**: Хабарландыруларды асинхронды өңдеу
- **Функциялары**:
  - RabbitMQ арқылы хабарландыруларды қабылдау
  - Брондау оқиғаларын өңдеу (растау, болдыру)
  - Хабарландыруларды жіберу

### Инфраструктуралық компоненттер:

#### 1. Frontend (Порт 3000)
- **Технология**: React 18, TypeScript, Vite, Tailwind CSS, Framer Motion
- **Функциялары**:
  - Заманауи пайдаланушы интерфейсі
  - Аутентификация (Keycloak интеграциясы)
  - Ресурстарды көрсету және басқару
  - Брондауларды құру және басқару
  - Адаптивті дизайн

#### 2. Keycloak (Порт 8088)
- **Рөлі**: Аутентификация және авторизация
- **Функциялары**:
  - Пайдаланушыларды басқару
  - JWT токендерді генерациялау
  - Рөлдерді басқару (admin, user)
  - OAuth2/OpenID Connect протоколдарын қолдау

#### 3. PostgreSQL
- **Рөлі**: Негізгі дерекқор
- **Порт**: 5434 (host), 5432 (контейнер ішінде)
- **Функциялары**:
  - Ресурстарды сақтау
  - Брондауларды сақтау
  - Транзакцияларды басқару

#### 4. RabbitMQ
- **Рөлі**: Хабарларды асинхронды тарату
- **Порттар**: 5672 (AMQP), 15672 (Management UI)
- **Функциялары**:
  - Хабарларды кезектеу
  - Микросервистер арасында хабарлама
  - Хабарландыруларды өңдеу

#### 5. Redis (Порт 6379)
- **Рөлі**: Кеш қабаты
- **Функциялары**:
  - Жиі сұралатын деректерді кештеу
  - Жауап беру жылдамдығын арттыру
  - Жүктемені азайту

#### 6. MinIO
- **Рөлі**: Файлдарды сақтау
- **Порттар**: 9000 (API), 9001 (Console)
- **Функциялары**:
  - Файлдарды жүктеу
  - Файлдарды жүктеп алу
  - Файлдарды жою
  - S3-совместимый API

#### 7. Prometheus (Порт 9090)
- **Рөлі**: Метрикаларды жинау
- **Функциялары**:
  - Сервистердің метрикаларын жинау
  - Деректерді сақтау
  - Сұрауларды орындау

#### 8. Grafana (Порт 3001)
- **Рөлі**: Метрикаларды визуализациялау
- **Функциялары**:
  - Дашбордтарды құру
  - Метрикаларды көрсету
  - Ескертулерді баптау

## ⚙️ Функционалдық мүмкіндіктер

### Resource Service

#### Ресурстарды басқару
- ✅ Ресурстарды құру (admin рөлі қажет)
- ✅ Барлық ресурстарды көру
- ✅ Ресурсты ID бойынша алу
- ✅ Ресурсты жаңарту (admin рөлі қажет)
- ✅ Ресурсты жою (admin рөлі қажет)

#### Сүзгілеу
- Тип бойынша сүзгілеу (MEETING_ROOM, CONFERENCE_HALL, EQUIPMENT, TRANSPORT)
- Қолжетімділік бойынша сүзгілеу (available=true/false)

#### Қолжетімділікті тексеру
- Ресурстың белгілі бір уақытта қолжетімді екенін тексеру

#### Кештеу
- Redis арқылы жиі сұралатын деректерді кештеу
- Кеш TTL: 1 сағат

#### Файлдармен жұмыс
- Ресурсқа файлдарды жүктеу
- Ресурстың файлдарын тізімдеу
- Файлдарды жүктеп алу
- Файлдарды жою (admin рөлі қажет)

### Booking Service

#### Брондауларды басқару
- ✅ Брондау құру (қолжетімділікті тексерумен)
- ✅ Пайдаланушының барлық брондауларын көру
- ✅ Брондауды ID бойынша алу
- ✅ Брондауды болдыру
- ✅ Ресурстың барлық брондауларын көру

#### Автоматикалық есептеулер
- Брондау бағасын автоматикалық есептеу (уақыт пен сағаттық баға бойынша)
- Resource Service-тен бағаны алу

#### Қолжетімділікті тексеру
- Ресурстың белгілі бір уақыт аралығында бос екенін тексеру

#### Кештеу
- Redis арқылы брондау деректерін кештеу

### Notification Service

#### Асинхронды өңдеу
- RabbitMQ арқылы хабарларды қабылдау
- Брондау оқиғаларын өңдеу:
  - Брондау расталғанда
  - Брондау болдырылғанда
- Хабарландыруларды жіберу

### File API

Барлық файл операциялары `/files` эндпоинті арқылы API Gateway-ден өтеді:

- `POST /files` - Файл жүктеу (авторизация қажет)
  - `multipart/form-data` форматы
  - `file` параметрі: жүктелетін файл
  - `resourceId` параметрі (міндетті емес): ресурс ID
  
- `GET /files` - Файлдар тізімін алу (авторизация қажет)
  - `resourceId` параметрі (міндетті емес): белгілі бір ресурстың файлдарын алу
  
- `GET /files/download` - Файлды жүктеп алу (авторизация қажет)
  - `name` параметрі: файл атауы
  - `resourceId` параметрі (міндетті емес): ресурс ID
  
- `DELETE /files/{objectName}` - Файлды жою (admin рөлі қажет)
  - `resourceId` параметрі (міндетті емес): ресурс ID

## 🛠️ Технологиялар

### Backend
- **Spring Boot 3.3.3** - Негізгі фреймворк
- **Spring Cloud Gateway** - API Gateway
- **Spring Data JPA** - Дерекқормен жұмыс
- **Spring Security OAuth2 Resource Server** - Қауіпсіздік
- **Maven** - Билд құралы

### Frontend
- **React 18** - UI кітапханасы
- **TypeScript** - Типтау
- **Vite** - Билд құралы
- **Tailwind CSS** - Стильдер
- **Framer Motion** - Анимациялар
- **Axios** - HTTP клиент

### Инфраструктура
- **PostgreSQL 15** - Дерекқор
- **RabbitMQ 3.13** - Хабарларды тарату
- **Redis 7** - Кеш
- **MinIO** - Файлдарды сақтау
- **Keycloak 24.0.5** - Аутентификация
- **Prometheus** - Метрикалар
- **Grafana** - Визуализация
- **Docker** - Контейнерлеу
- **Docker Compose** - Оркестрация

## 📦 Қажетті құралдар

### Міндетті
1. **Java 17+** - Backend сервистер үшін
2. **Maven 3.8+** - Билд үшін
3. **Docker** - Контейнерлерді іске қосу үшін
4. **Docker Compose** - Сервистерді оркестрациялау үшін
5. **Node.js 18+** - Frontend үшін (локальды дамыту үшін)

### Қосымша (опционал)
- **Postman** немесе **curl** - API тестілеу үшін
- **IDE** (IntelliJ IDEA, VS Code) - Код жазу үшін

## 🚀 Орнату және іске қосу

### 1. Проектті клонирлеу

```bash
git clone <repository-url>
cd "Java project Aidana"
```

### 2. Java және Maven тексеру

```bash
java -version  # Java 17+ болуы керек
mvn -version  # Maven 3.8+ болуы керек
```

### 3. Проектті билдлеу

#### Windows (PowerShell):
```powershell
mvn -pl services/api-gateway,services/file-service,services/service-two,services/data-processor clean package -DskipTests
```

#### Linux/Mac:
```bash
mvn -pl services/api-gateway,services/file-service,services/service-two,services/data-processor clean package -DskipTests
```

Бұл команда барлық микросервистерді билдлейді және JAR файлдарын `target/` директориясына орналастырады.

### 4. Frontend билдлеу (Docker үшін)

#### Windows (PowerShell):
```powershell
cd frontend
npm install
npm run build
cd ..
```

#### Linux/Mac:
```bash
cd frontend
npm install
npm run build
cd ..
```

**Ескерту**: Егер сіз frontend-ті Docker-сыз локальды іске қоссаңыз, бұл қадамды өткізіп жіберуге болады.

### 5. Docker Compose арқылы іске қосу

#### Windows (PowerShell):
```powershell
cd infra/docker
docker compose up -d
```

#### Linux/Mac:
```bash
cd infra/docker
docker compose up -d
```

Бұл команда барлық сервистерді контейнерлерде іске қосады:
- PostgreSQL
- Keycloak
- MinIO
- RabbitMQ
- Redis
- Prometheus
- Grafana
- API Gateway
- Resource Service
- Booking Service
- Notification Service
- Frontend

### 6. Сервистердің дайын болуын күту

Сервистер іске қосылу үшін біраз уақыт алады. Статусты тексеру:

```bash
docker compose ps
```

Барлық сервистер `Up` статусында болуы керек.

### 7. Логтарды тексеру

Егер мәселелер болса, логтарды қараңыз:

```bash
# Барлық сервистердің логтары
docker compose logs

# Белгілі бір сервистің логтары
docker compose logs api-gateway
docker compose logs resource-service
docker compose logs booking-service
```

## ⚙️ Конфигурация

### Keycloak баптау

1. **Keycloak-қа кіру**:
   - URL: http://localhost:8088
   - Логин: `admin`
   - Пароль: `admin`

2. **Realm тексеру**:
   - Realm "app" автоматты түрде импортталады
   - Егер жоқ болса, оны қолмен құруға болады

3. **Клиенттерді тексеру**:
   - `api-gateway` клиенті автоматты түрде құрылады
   - Client ID: `api-gateway`
   - Client Secret: `gateway-secret`

4. **Пайдаланушыларды құру**:
   - Admin панелінен "Users" → "Add user"
   - Username: `admin` (немесе басқа)
   - Email: `admin@example.com`
   - "Credentials" тармағынан парольді орнату
   - "Role mappings" тармағынан "admin" рөлін беру

### Environment айнымалылары

Docker Compose файлында (`infra/docker/docker-compose.yml`) келесі айнымалыларды өзгертуге болады:

#### API Gateway
- `KEYCLOAK_ISSUER_URI` - Keycloak issuer URI
- `RESOURCE_SERVICE_URL` - Resource Service URL
- `BOOKING_SERVICE_URL` - Booking Service URL
- `NOTIFICATION_SERVICE_URL` - Notification Service URL

#### Resource Service
- `DB_URL` - PostgreSQL қосылым URL
- `DB_USER` - Дерекқор пайдаланушысы
- `DB_PASSWORD` - Дерекқор паролі
- `KEYCLOAK_ISSUER_URI` - Keycloak issuer URI
- `MINIO_URL` - MinIO сервер URL
- `MINIO_BUCKET` - MinIO бакет атауы
- `MINIO_ACCESS_KEY` - MinIO access key
- `MINIO_SECRET_KEY` - MinIO secret key
- `RABBITMQ_HOST` - RabbitMQ хост
- `REDIS_HOST` - Redis хост

#### Booking Service
- `DB_URL` - PostgreSQL қосылым URL
- `DB_USER` - Дерекқор пайдаланушысы
- `DB_PASSWORD` - Дерекқор паролі
- `KEYCLOAK_ISSUER_URI` - Keycloak issuer URI
- `RESOURCE_SERVICE_URL` - Resource Service URL
- `RABBITMQ_HOST` - RabbitMQ хост
- `REDIS_HOST` - Redis хост
- `APP_QUEUE` - RabbitMQ кезек атауы

### PostgreSQL баптау

Дерекқор автоматты түрде құрылады:
- **Database**: `platform`
- **User**: `platform`
- **Password**: `platform`
- **Port**: `5434` (host), `5432` (контейнер ішінде)

### Redis баптау

Redis автоматты түрде іске қосылады:
- **Host**: `redis`
- **Port**: `6379`
- **Кеш типі**: Redis (`spring.cache.type=redis`)

### MinIO баптау

MinIO автоматты түрде іске қосылады:
- **API URL**: http://localhost:9000
- **Console URL**: http://localhost:9001
- **Access Key**: `minioadmin`
- **Secret Key**: `minioadmin`
- **Bucket**: `files` (автоматты түрде құрылады)

## 📡 API Эндпоинттер

### Resource Service (API Gateway арқылы: `/resources`)

#### Ресурстарды алу
```http
GET /resources
Authorization: Bearer <token>
```

**Сүзгілеу параметрлері**:
- `type` - Ресурс типі (MEETING_ROOM, CONFERENCE_HALL, EQUIPMENT, TRANSPORT)
- `available` - Қолжетімділік (true/false)

**Мысал**:
```http
GET /resources?type=MEETING_ROOM&available=true
Authorization: Bearer <token>
```

#### Ресурсты ID бойынша алу
```http
GET /resources/{id}
Authorization: Bearer <token>
```

#### Ресурс құру (admin рөлі қажет)
```http
POST /resources
Authorization: Bearer <token>
Content-Type: application/json

{
  "name": "Кеңес залы A",
  "description": "Үлкен кездесу залы",
  "type": "CONFERENCE_HALL",
  "pricePerHour": 5000.00,
  "capacity": 50,
  "available": true
}
```

#### Ресурсты жаңарту (admin рөлі қажет)
```http
PUT /resources/{id}
Authorization: Bearer <token>
Content-Type: application/json

{
  "name": "Кеңес залы A (жаңартылған)",
  "description": "Жаңартылған сипаттама",
  "type": "CONFERENCE_HALL",
  "pricePerHour": 5500.00,
  "capacity": 60,
  "available": true
}
```

#### Ресурсты жою (admin рөлі қажет)
```http
DELETE /resources/{id}
Authorization: Bearer <token>
```

#### Ресурстың қолжетімділігін тексеру
```http
GET /resources/{id}/availability
Authorization: Bearer <token>
```

### Booking Service (API Gateway арқылы: `/bookings`)

#### Брондауларды алу
```http
GET /bookings
Authorization: Bearer <token>
```

#### Брондауды ID бойынша алу
```http
GET /bookings/{id}
Authorization: Bearer <token>
```

#### Ресурстың брондауларын алу
```http
GET /bookings/resource/{resourceId}
Authorization: Bearer <token>
```

#### Брондау құру
```http
POST /bookings
Authorization: Bearer <token>
Content-Type: application/json

{
  "resourceId": 1,
  "startTime": "2025-12-15T10:00:00",
  "endTime": "2025-12-15T12:00:00",
  "notes": "Команда кездесуі"
}
```

#### Брондауды болдыру
```http
DELETE /bookings/{id}
Authorization: Bearer <token>
```

#### Қолжетімділікті тексеру
```http
GET /bookings/availability?resourceId=1&startTime=2025-12-15T10:00:00&endTime=2025-12-15T12:00:00
Authorization: Bearer <token>
```

### File API (API Gateway арқылы: `/files`)

#### Файл жүктеу
```http
POST /files
Authorization: Bearer <token>
Content-Type: multipart/form-data

file: <файл>
resourceId: 1 (міндетті емес)
```

#### Файлдар тізімін алу
```http
GET /files?resourceId=1
Authorization: Bearer <token>
```

#### Файлды жүктеп алу
```http
GET /files/download?name=example.pdf&resourceId=1
Authorization: Bearer <token>
```

#### Файлды жою (admin рөлі қажет)
```http
DELETE /files/{objectName}?resourceId=1
Authorization: Bearer <token>
```

## 💡 Мысалдар

### 1. Токен алу

#### PowerShell:
```powershell
$resp = curl.exe -s -X POST "http://localhost:8088/realms/app/protocol/openid-connect/token" `
  -d "client_id=api-gateway" `
  -d "client_secret=gateway-secret" `
  -d "grant_type=password" `
  -d "username=admin" `
  -d "password=admin"
$token = ($resp | ConvertFrom-Json).access_token
Write-Host "Token: $token"
```

#### Bash:
```bash
TOKEN=$(curl -s -X POST "http://localhost:8088/realms/app/protocol/openid-connect/token" \
  -d "client_id=api-gateway" \
  -d "client_secret=gateway-secret" \
  -d "grant_type=password" \
  -d "username=admin" \
  -d "password=admin" | jq -r '.access_token')
echo "Token: $TOKEN"
```

### 2. Ресурс құру

#### PowerShell:
```powershell
$headers = @{
    "Authorization" = "Bearer $token"
    "Content-Type" = "application/json"
}
$body = @{
    name = "Кеңес залы A"
    description = "Үлкен кездесу залы"
    type = "CONFERENCE_HALL"
    pricePerHour = 5000.00
    capacity = 50
    available = $true
} | ConvertTo-Json

curl.exe -X POST "http://localhost:8080/resources" `
  -H $headers[0] `
  -H $headers[1] `
  -d $body
```

#### Bash:
```bash
curl -X POST "http://localhost:8080/resources" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Кеңес залы A",
    "description": "Үлкен кездесу залы",
    "type": "CONFERENCE_HALL",
    "pricePerHour": 5000.00,
    "capacity": 50,
    "available": true
  }'
```

### 3. Брондау құру

#### PowerShell:
```powershell
$body = @{
    resourceId = 1
    startTime = "2025-12-15T10:00:00"
    endTime = "2025-12-15T12:00:00"
    notes = "Команда кездесуі"
} | ConvertTo-Json

curl.exe -X POST "http://localhost:8080/bookings" `
  -H "Authorization: Bearer $token" `
  -H "Content-Type: application/json" `
  -d $body
```

#### Bash:
```bash
curl -X POST "http://localhost:8080/bookings" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "resourceId": 1,
    "startTime": "2025-12-15T10:00:00",
    "endTime": "2025-12-15T12:00:00",
    "notes": "Команда кездесуі"
  }'
```

### 4. Файл жүктеу

#### PowerShell:
```powershell
$filePath = "C:\path\to\file.pdf"
$form = @{
    file = Get-Item -Path $filePath
    resourceId = 1
}

curl.exe -X POST "http://localhost:8080/files" `
  -H "Authorization: Bearer $token" `
  -F "file=@$filePath" `
  -F "resourceId=1"
```

#### Bash:
```bash
curl -X POST "http://localhost:8080/files" \
  -H "Authorization: Bearer $TOKEN" \
  -F "file=@/path/to/file.pdf" \
  -F "resourceId=1"
```

## 🌐 Сервистерге қолжетімділік

Барлық сервистер Docker Compose арқылы іске қосылғаннан кейін келесі адрестер арқылы қолжетімді:

| Сервис | URL | Логин/Пароль |
|--------|-----|--------------|
| **Frontend** | http://localhost:3000 | - |
| **API Gateway** | http://localhost:8080 | - |
| **Keycloak** | http://localhost:8088 | admin/admin |
| **RabbitMQ Management** | http://localhost:15672 | guest/guest |
| **MinIO Console** | http://localhost:9001 | minioadmin/minioadmin |
| **Prometheus** | http://localhost:9090 | - |
| **Grafana** | http://localhost:3001 | admin/admin |

### Health Check эндпоинттер

Барлық сервистерде health check эндпоинттер бар:

```http
GET /actuator/health
```

**Мысал**:
```bash
curl http://localhost:8080/actuator/health
```

### Prometheus метрикалары

Барлық сервистер Prometheus метрикаларын `/actuator/prometheus` эндпоинті арқылы көрсетеді:

```http
GET /actuator/prometheus
```

## 🔧 Мәселелерді шешу

### Сервистер іске қосылмайды

1. **Docker статусын тексеру**:
   ```bash
   docker compose ps
   ```

2. **Логтарды қарау**:
   ```bash
   docker compose logs <service-name>
   ```

3. **Порттардың бос екенін тексеру**:
   - 3000, 8080, 8081, 8082, 8083, 8088, 5434, 5672, 6379, 9000, 9001, 9090, 3001

### Keycloak-қа қосылу мәселелері

1. **Keycloak-тың дайын болуын күту** (30-60 секунд)
2. **Realm "app" құрылғанын тексеру**
3. **Клиент "api-gateway" құрылғанын тексеру**

### Дерекқор қосылым мәселелері

1. **PostgreSQL контейнерінің іске қосылғанын тексеру**:
   ```bash
   docker compose ps postgres
   ```

2. **Дерекқорға тікелей қосылу**:
   ```bash
   docker compose exec postgres psql -U platform -d platform
   ```

### Redis қосылым мәселелері

1. **Redis контейнерінің іске қосылғанын тексеру**:
   ```bash
   docker compose ps redis
   ```

2. **Redis-қа тікелей қосылу**:
   ```bash
   docker compose exec redis redis-cli ping
   ```

### MinIO мәселелері

1. **MinIO контейнерінің іске қосылғанын тексеру**
2. **MinIO Console-ға кіру**: http://localhost:9001
3. **Bucket "files" құрылғанын тексеру**

### Сервистерді қайта іске қосу

```bash
# Барлық сервистерді тоқтату
docker compose down

# Барлық сервистерді қайта іске қосу
docker compose up -d

# Белгілі бір сервисті қайта іске қосу
docker compose restart <service-name>
```

### Деректерді тазалау

**ЕСКЕРТУ**: Бұл операция барлық деректерді жояды!

```bash
# Контейнерлерді тоқтату және жою
docker compose down -v

# Қайта іске қосу
docker compose up -d
```

### Порттар бос емес

Егер порттар бос емес болса, `docker-compose.yml` файлында порттарды өзгертуге болады:

```yaml
ports:
  - "8080:8080"  # Сол жақ: host порты, оң жақ: контейнер порты
```

## 📚 Қосымша ақпарат

### Локальды дамыту (Docker-сыз)

Егер сіз сервистерді Docker-сыз локальды іске қосқыңыз келсе:

1. **PostgreSQL, Redis, RabbitMQ, MinIO, Keycloak-ты локальды орнату**
2. **Environment айнымалыларын баптау**
3. **Сервистерді IDE-ден іске қосу**

### Frontend локальды дамыту

```bash
cd frontend
npm install
npm run dev
```

Frontend http://localhost:3000 адресінде іске қосылады (Vite dev server).

### Билд мәселелері

Егер Maven билд мәселелері болса:

1. **Maven кешін тазалау**:
   ```bash
   mvn clean
   ```

2. **Зависимостілерді қайта жүктеу**:
   ```bash
   mvn dependency:resolve
   ```

3. **Белгілі бір сервисті билдлеу**:
   ```bash
   cd services/api-gateway
   mvn clean package -DskipTests
   ```

## 📞 Қолдау

Егер мәселелер болса:
1. Логтарды тексеру: `docker compose logs`
2. Сервистердің статусын тексеру: `docker compose ps`
3. Health check эндпоинттерін тексеру
4. Дерекқорға қосылуды тексеру

---

**Жоба дайын! Барлық сервистер Docker Compose арқылы іске қосылады және жұмыс істейді.** 🚀
