# Booking Platform Frontend

Современный фронтенд для системы бронирования, построенный на React + TypeScript с использованием Vite, Tailwind CSS и Framer Motion.

## Возможности

- 🎨 Современный и красивый UI с анимациями
- 🔐 Интеграция с Keycloak для аутентификации
- 📱 Адаптивный дизайн
- ⚡ Быстрая загрузка благодаря Vite
- 🎭 Плавные анимации с Framer Motion
- 📊 Панель управления с статистикой
- 📅 Управление бронированиями
- 🏢 Просмотр и фильтрация ресурсов

## Технологии

- **React 18** - UI библиотека
- **TypeScript** - Типизация
- **Vite** - Сборщик и dev-сервер
- **Tailwind CSS** - Стилизация
- **Framer Motion** - Анимации
- **React Router** - Маршрутизация
- **Axios** - HTTP клиент
- **React Hot Toast** - Уведомления
- **Lucide React** - Иконки
- **date-fns** - Работа с датами

## Установка и запуск

### Локальная разработка

```bash
# Установить зависимости
npm install

# Запустить dev-сервер
npm run dev

# Собрать для production
npm run build

# Предпросмотр production сборки
npm run preview
```

### Docker

```bash
# Собрать образ
docker build -t booking-frontend .

# Запустить контейнер
docker run -p 3000:80 booking-frontend
```

## Переменные окружения

Создайте файл `.env` в корне проекта:

```env
VITE_API_URL=http://localhost:8080
VITE_KEYCLOAK_URL=http://localhost:8088
```

## Структура проекта

```
frontend/
├── src/
│   ├── components/      # Переиспользуемые компоненты
│   ├── contexts/        # React контексты (Auth)
│   ├── pages/           # Страницы приложения
│   ├── services/        # API сервисы
│   ├── App.tsx          # Главный компонент
│   └── main.tsx         # Точка входа
├── public/              # Статические файлы
├── index.html           # HTML шаблон
└── package.json         # Зависимости
```

## Страницы

- **/** - Главная страница с панелью управления
- **/login** - Страница входа
- **/resources** - Список ресурсов
- **/bookings** - Мои бронирования
- **/bookings/create** - Создание бронирования

## API Интеграция

Фронтенд взаимодействует с API Gateway на `http://localhost:8080`:

- `GET /resources` - Получить все ресурсы
- `GET /resources/{id}` - Получить ресурс по ID
- `GET /bookings` - Получить все бронирования пользователя
- `POST /bookings` - Создать бронирование
- `DELETE /bookings/{id}` - Отменить бронирование
- `GET /bookings/availability` - Проверить доступность

## Аутентификация

Аутентификация происходит через Keycloak. После успешного входа токен сохраняется в localStorage и добавляется в заголовки всех запросов.

## Разработка

### Добавление новой страницы

1. Создайте файл в `src/pages/`
2. Добавьте маршрут в `src/App.tsx`
3. Добавьте ссылку в навигацию в `src/components/Layout.tsx`

### Стилизация

Используйте Tailwind CSS классы. Кастомные стили можно добавить в `src/index.css`.

### Анимации

Используйте Framer Motion для анимаций:

```tsx
import { motion } from 'framer-motion'

<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.5 }}
>
  Content
</motion.div>
```







